version: "3.8"

services:
  redis:
    image: redis:alpine
    container_name: anytimerequest-redis-dev
    ports:
      - "6379:6379"
    volumes:
      - redis_dev_data:/data
    restart: always

volumes:
  redis_dev_data:
